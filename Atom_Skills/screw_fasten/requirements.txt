# RoboForce Solar Panel Screw Installation - Dependencies
# ======================================================
# Install with: pip install -r requirements.txt
# Assumes IsaacLab 2.3.2 is already installed at /home/siyu/IsaacLab/

# Isaac Lab / Sim (installed separately via IsaacLab repo)
# isaacsim >= 4.5.0
# isaaclab >= 2.3.2

# Core
numpy>=1.24.0,<2.0
scipy>=1.10.0
torch>=2.1.0
torchvision>=0.16.0

# Configuration
pyyaml>=6.0
hydra-core>=1.3.0
omegaconf>=2.3.0

# Data collection & formats
h5py>=3.8.0
pyarrow>=12.0.0
datasets>=2.14.0  # HuggingFace datasets (LeRobot V2 format)
lerobot>=0.1.0    # LeRobot data utilities

# Computer vision & labeling
opencv-python>=4.8.0
pycocotools>=2.0.7
Pillow>=10.0.0

# Visualization & reporting
matplotlib>=3.7.0
tensorboard>=2.14.0
tqdm>=4.65.0
tabulate>=0.9.0

# Evaluation & benchmarking
psutil>=5.9.0

# GR00T N1.6 (install from source)
# git clone https://github.com/NVIDIA/Isaac-GR00T
# cd Isaac-GR00T && pip install -e .

# OpenPI / π₀ (install from source)
# git clone https://github.com/Physical-Intelligence/openpi
# cd openpi && pip install -e .
